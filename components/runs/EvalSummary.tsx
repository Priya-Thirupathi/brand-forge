// GET /api/eval/summary is an empty stub until Stage 2's TypeScript eval harness exists.
export function EvalSummary() {
  return (
    <div className="rounded border border-dashed border-zinc-300 p-4 text-sm text-zinc-500 dark:border-zinc-700">
      Evaluation summary isn&apos;t built yet — it lands in Stage 2 with the eval harness.
    </div>
  );
}
